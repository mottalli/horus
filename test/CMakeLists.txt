SET(SOURCES main.cpp ${HORUS_LIB_SOURCES})

ADD_EXECUTABLE(test ${SOURCES})
TARGET_LINK_LIBRARIES(test ${OpenCV_LIBS} ${Boost_LIBRARIES})

IF (USE_CUDA)
	TARGET_LINK_LIBRARIES(test horus_cuda)
ENDIF (USE_CUDA)
